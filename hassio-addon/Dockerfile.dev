FROM ghcr.io/tomquist/hame-relay-addon:latest

# Optional: Arbeitsverzeichnis
WORKDIR /app

FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build

CMD ["node", "dist/forwarder.js"]

